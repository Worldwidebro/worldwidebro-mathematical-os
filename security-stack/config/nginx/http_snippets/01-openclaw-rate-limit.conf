# Include once from nginx.conf inside the http { } block, e.g.:
#   include /etc/nginx/snippets/security-stack/01-openclaw-rate-limit.conf;

limit_req_zone $binary_remote_addr zone=openclaw_limit:10m rate=10r/s;
