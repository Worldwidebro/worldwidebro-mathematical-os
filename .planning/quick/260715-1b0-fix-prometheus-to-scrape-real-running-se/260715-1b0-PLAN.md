---
phase: quick-260715-1b0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/acebless/Documents/prometheus.yml
autonomous: true
requirements: [QUICK-1b0]
must_haves:
  truths:
    - "Prometheus scrapes more than 1 active target (itself + at least Grafana + Qdrant)"
    - "Grafana native /metrics endpoint is scraped and reachable over the ventures network"
    - "Qdrant native /metrics endpoint is scraped and reachable over the ventures network"
    - "Config reload/restart succeeds and the new targets appear in the targets API"
  artifacts:
    - /Users/acebless/Documents/prometheus.yml
  key_links:
    - "prometheus container -> grafana:3000/metrics (docker network, container port not host port)"
    - "prometheus container -> qdrant:6333/metrics (docker network)"
---

<objective>
Prometheus (prom/prometheus:v2.53.0, container `prometheus`, host :9090) currently reports exactly 1 active target: itself. Add scrape configs for the services in the local Docker Compose stack (`/Users/acebless/Documents/docker-compose.yml`) that already expose a native Prometheus `/metrics` endpoint — verified: Grafana and Qdrant — then reload/restart Prometheus and confirm via the targets API that more than 1 target is active.

Purpose: Make the observability stack actually observe the running services instead of only itself.
Output: Updated `prometheus.yml` with real scrape jobs + a verified targets API response showing multiple active targets.
</objective>

<execution_context>
@/Users/acebless/Documents/.claude/gsd-core/workflows/execute-plan.md
@/Users/acebless/Documents/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/acebless/Documents/docker-compose.yml
@/Users/acebless/Documents/prometheus.yml

Key facts established during planning (do not re-derive):
- `prometheus.yml` is a HOST BIND-MOUNT: `./prometheus.yml:/etc/prometheus/prometheus.yml`. Editing the file on the host at `/Users/acebless/Documents/prometheus.yml` and reloading/restarting the container is the correct approach (no volume-copy needed).
- Prometheus and all stack services share the `ventures` Docker bridge network. Prometheus MUST address other services by their compose service name and CONTAINER port, NOT the host-published port. Specifically:
    - Grafana:  `grafana:3000`  (host publishes 3001:3000 — use 3000 internally)
    - Qdrant:   `qdrant:6333`   (host publishes 6333:6333)
- Native /metrics confirmed present (HTTP 200) during planning: Grafana (:3001/metrics on host), Qdrant (:6333/metrics on host).
- The on-disk `prometheus.yml` already contains a `prometheus` job AND an `otel-collector` job (`otel-collector:9464`), but the live targets API shows only `prometheus` — meaning the config was edited previously but never reloaded. Reloading will also activate the existing otel-collector job.
- Prometheus is started WITHOUT `--web.enable-lifecycle`, so the POST /-/reload HTTP API is NOT available. Reload must be done via SIGHUP (`docker exec prometheus kill -HUP 1`) or a container restart (`docker compose restart prometheus`).
- Docker daemon on this machine showed intermittent disk I/O errors during planning. Prefer `docker compose restart prometheus` as the primary reload method; if the daemon is unhealthy, retry once and surface the error rather than looping.

Services intentionally SKIPPED (out of scope for this quick task) — document these in the SUMMARY:
- Redis: needs a `redis_exporter` sidecar (no native Prometheus endpoint).
- Postgres: needs a `postgres_exporter` sidecar (no native Prometheus endpoint).
- Neo4j: Community edition; metrics endpoint is an Enterprise/config feature — not exposed here.
- LiteLLM: `/metrics` returned HTTP 404 during planning (LiteLLM Prometheus metrics are license/enterprise-gated) — no valid endpoint to scrape.
- n8n: metrics require setting `N8N_METRICS=true` env + n8n restart (a compose change beyond this task's scope).
- otel-collector: already present in config; left as-is (will activate on reload).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Grafana and Qdrant scrape jobs to prometheus.yml</name>
  <files>/Users/acebless/Documents/prometheus.yml</files>
  <action>
Edit the host file `/Users/acebless/Documents/prometheus.yml` (bind-mounted into the container). Preserve the existing `global`, `prometheus`, and `otel-collector` scrape configs — do NOT remove them. Append two new jobs under `scrape_configs`:
- A job named `grafana` with a single static target `grafana:3000` (metrics_path defaults to /metrics, which Grafana serves natively).
- A job named `qdrant` with a single static target `qdrant:6333` (Qdrant serves Prometheus metrics at /metrics natively).
Use the compose service names and container ports exactly as listed in the context (NOT host ports). Keep YAML indentation consistent with the existing file (two-space list items under `scrape_configs`). Do not add auth, TLS, or relabeling — these endpoints are plain HTTP on the internal `ventures` network.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('/Users/acebless/Documents/prometheus.yml')); jobs={j['job_name']: [t for sc in j.get('static_configs',[]) for t in sc['targets']] for j in d['scrape_configs']}; assert jobs.get('grafana')==['grafana:3000'], jobs; assert jobs.get('qdrant')==['qdrant:6333'], jobs; assert 'prometheus' in jobs, jobs; print('OK', jobs)"</automated>
  </verify>
  <done>prometheus.yml parses as valid YAML and contains `grafana` (target grafana:3000) and `qdrant` (target qdrant:6333) jobs alongside the pre-existing prometheus and otel-collector jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Reload Prometheus and verify multiple active targets</name>
  <files>/Users/acebless/Documents/prometheus.yml</files>
  <action>
Reload the Prometheus config so it picks up the new jobs. Primary method: run `docker compose -f /Users/acebless/Documents/docker-compose.yml restart prometheus`. If that fails due to a docker daemon I/O error, retry once; if it still fails, try the SIGHUP reload `docker exec prometheus kill -HUP 1` (works because prometheus runs as PID 1). Do NOT attempt the HTTP `/-/reload` API — the lifecycle flag is not enabled on this container. After reload, wait a few seconds for the first scrape cycle, then query the targets API. If neither reload method works because the docker daemon is unhealthy, surface the daemon error clearly in the SUMMARY rather than marking success.
  </action>
  <verify>
    <automated>sleep 8; curl -s http://localhost:9090/api/v1/targets | python3 -c "import sys,json; d=json.load(sys.stdin); at=d['data']['activeTargets']; jobs=sorted({t['labels']['job'] for t in at}); print('active_targets=',len(at),'jobs=',jobs); assert len(at)>1, 'still only 1 target'; assert 'grafana' in jobs and 'qdrant' in jobs, jobs; print('OK')"</automated>
  </verify>
  <done>`curl http://localhost:9090/api/v1/targets` reports more than 1 active target, and the job set includes `grafana` and `qdrant`. (Grafana and Qdrant should show health=up; otel-collector may show up or down depending on its exporter — either is acceptable as long as total active targets > 1.)</done>
</task>

</tasks>

<verification>
- `prometheus.yml` on host contains 4 scrape jobs: prometheus, otel-collector (pre-existing), grafana, qdrant (new).
- `curl -s http://localhost:9090/api/v1/targets` returns activeTargets length > 1, including grafana and qdrant, with grafana/qdrant health=up.
- SUMMARY documents which services were skipped and why (Redis/Postgres need exporter sidecars; Neo4j/LiteLLM metrics are enterprise/license-gated; n8n needs N8N_METRICS env).
</verification>

<success_criteria>
Prometheus scrapes at least itself + Grafana + Qdrant (>1 active target), verified via the targets API, using host-side edits to the bind-mounted prometheus.yml plus a container reload/restart. Skipped services are clearly documented.
</success_criteria>

<output>
Create `.planning/quick/260715-1b0-fix-prometheus-to-scrape-real-running-se/260715-1b0-SUMMARY.md` when done.
</output>
