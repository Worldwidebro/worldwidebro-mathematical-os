---
phase: 01-launch-campaigns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ops-001-staffing/src/**
  - con-001-ace-construction/src/**
  - ec-112-cosmic-kitty/src/**
  - .supabase/migrations/**
  - .vercel/env/**
autonomous: false
requirements:
  - OPS-001-revenue-ready
  - B001-resolved
  - B002-resolved
  - B003-resolved
  - B004-resolved
  - EC-112-ads-live
  - CON-001-ads-live
  - 3-ventures-deployed-vercel

estimate:
  tokens: 280000
  raw_tokens: 140000
  tasks: 12
  confidence: med

must_haves:
  truths:
    - OPS-001 staffing requests are receivable via web portal and SMS
    - EC-112 paid campaigns are live on Facebook and Google
    - CON-001 paid campaigns are live on Facebook and Google
    - All 3 ventures (OPS-001, CON-001, EC-112) are deployed to Vercel with production URLs
    - Supabase property/tenant model supports RE-001 data storage and retrieval
    - Stripe webhooks process payment events from EC-112 and CON-001
    - All venture deployments have correct Vercel env vars (Supabase keys, Stripe secret)
    - Portfolio readiness improves from 27.7% to 35%+
  artifacts:
    - ops-001-staffing-portal live on Vercel (https://ops-001.vercel.app)
    - con-001-ace-construction live on Vercel (https://con-001.vercel.app)
    - ec-112-cosmic-kitty live on Vercel (https://ec-112.vercel.app)
    - Supabase tables: properties, tenants, leases (with RLS enabled)
    - Supabase table: payments (with RLS and Stripe webhook integration)
    - VAPI webhook endpoint for SMS dispatch (ops-001 and lt-005)
    - Stripe webhook endpoint for payment processing
  key_links:
    - Vercel deployments → Supabase (via env vars: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY)
    - Stripe → Supabase (webhook writes to payments table)
    - VAPI/Twilio → Supabase (SMS dispatch logs to calls table)
    - Ad platforms (Facebook/Google) → Landing pages (conversion tracking pixels)
    - OPS-001 portal → VAPI (call routing)

---

<objective>
**As a** portfolio operator managing 712 ventures  
**I want to** launch 3 revenue-generating campaigns + resolve 4 critical blockers  
**So that** $50K revenue is activated in Week 1, unblocking synergies and proving the operating model

**Purpose:** Establish proof of parallel execution — campaigns + blockers — simultaneously across ventures with shared infrastructure (Supabase, Vercel, Stripe). Week 1 serves as the critical path validator for Phases 2-4.

**Output:** 3 live ventures (OPS-001, CON-001, EC-112) on Vercel generating revenue via campaigns, 4 blockers resolved enabling Phases 2+ synergies, portfolio readiness 27.7% → 35%.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/acebless/Documents/.planning/ROADMAP.md
@/Users/acebless/Documents/.planning/STATE.md
@/Users/acebless/Documents/PORTFOLIO-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-End: Deploy OPS-001 Staffing Portal (Vercel + Supabase + Call Routing)</name>
  <files>
    ops-001-staffing/package.json
    ops-001-staffing/vercel.json
    ops-001-staffing/src/pages/api/staffing/requests.ts
    ops-001-staffing/src/pages/api/calls/vapi.ts
    ops-001-staffing/src/components/StaffingPortal.tsx
    ops-001-staffing/src/lib/supabase.ts
    .env.production (Vercel project)
    .supabase/migrations/001_staffing_tables.sql
  </files>
  <action>
Wire OPS-001 staffing portal end-to-end: Create Supabase staffing/calls tables with RLS (schema.sql), create Next.js API endpoints for staffing requests + VAPI webhook, create React portal UI (list requests, assign staff, dial call), deploy to Vercel with env vars (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, VAPI_WEBHOOK_SECRET), test one complete flow: user submits request via web portal → API routes to Supabase → VAPI call webhook fires → staff receives call via Twilio. No other ventures wired yet—this is the single proven slice. Real error handling on all paths.
  </action>
  <verify>
    <automated>
curl -X POST https://ops-001.vercel.app/api/staffing/requests \
  -H "Content-Type: application/json" \
  -d '{"name":"Test","phone":"+15551234567"}' | grep -q '"id"' && echo "PASS: Request created" || echo "FAIL"

curl https://ops-001.vercel.app/api/health | grep -q '"status":"ok"' && echo "PASS: API live" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM staffing_requests;" | grep -q "1" && echo "PASS: DB written" || echo "FAIL"
    </automated>
  </verify>
  <done>
OPS-001 live on Vercel at production URL, staffing request can be created via API, Supabase tables initialized with RLS, one end-to-end call flow validated (portal → DB → VAPI). Committed as "feat(01-01): deploy OPS-001 staffing portal (tracer)".
  </done>
  <precondition>Vercel CLI authenticated to worldwidebro org; Supabase CLI authenticated to con-001-ace-construction project</precondition>
</task>

<task type="auto">
  <name>B004: Configure Vercel Environment Variables (All Ventures)</name>
  <files>
    vercel.json (each venture)
    .env.production (committed reference only)
  </files>
  <action>
Set Vercel env vars for all 3 ventures (ops-001-staffing, con-001-ace-construction, ec-112-cosmic-kitty) via CLI: `vercel env add SUPABASE_URL "<url>" production`, `vercel env add SUPABASE_ANON_KEY "<key>" production`, `vercel env add SUPABASE_SERVICE_ROLE_KEY "<key>" production`, `vercel env add STRIPE_SECRET_KEY "<key>" production`. Use Supabase project con-001-ace-construction (shared by all ventures for Phase 1). Verify each venture's build succeeds with env vars set.
  </action>
  <verify>
    <automated>
for venture in "ops-001-staffing" "con-001-ace-construction" "ec-112-cosmic-kitty"; do
  vercel env list --cwd "$venture" | grep -q "SUPABASE_URL" && echo "PASS: $venture has SUPABASE_URL" || echo "FAIL: $venture missing SUPABASE_URL"
done

vercel build --cwd ops-001-staffing 2>&1 | grep -q "Build succeeded" && echo "PASS: OPS-001 build succeeded" || echo "FAIL"
    </automated>
  </verify>
  <done>
All 3 ventures have env vars set in Vercel production environment; each venture's build completes without errors. No deployment to production yet—this unblocks B001/B002/B003 resolution.
  </done>
  <precondition>Vercel CLI authenticated; Supabase keys copied to safe storage; Stripe secret key available</precondition>
  <tag>MUST-HAVE</tag>
</task>

<task type="auto">
  <name>B001: Supabase Property/Tenant Data Model + RLS Policies</name>
  <files>
    .supabase/migrations/002_property_tenant_model.sql
    .supabase/migrations/003_property_rls.sql
  </files>
  <action>
Create Supabase schema for properties/tenants/leases (B001 unblocks RE-001 $900K + LT-005 space lease synergy). Tables: `properties` (id, address, city, owner_id, valuations_json, created_at), `tenants` (id, property_id, lease_start, lease_end, monthly_rent, company_name), `leases` (id, property_id, tenant_id, signed_date, term_months). RLS: tenants can read only their own leases; staff can read all properties; admins full access. Run migrations via `supabase db push`. Seed 3 test properties + 5 test tenants for RE-001 validation. No frontend wiring yet—schema only.
  </action>
  <verify>
    <automated>
psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_name IN ('properties', 'tenants', 'leases');" | grep -q "3" && echo "PASS: All 3 tables exist" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM properties;" | grep -q "3" && echo "PASS: Test properties seeded" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT * FROM pg_policies WHERE tablename = 'leases' AND qual ~ 'auth.uid';" | grep -q "rls" && echo "PASS: RLS enabled on leases" || echo "FAIL"
    </automated>
  </verify>
  <done>
Supabase schema for properties/tenants/leases deployed; RLS policies enforced; test data seeded. RE-001 team can now iterate on property management UI without frontend/backend blocking data model.
  </done>
  <precondition>Supabase CLI authenticated to con-001-ace-construction project</precondition>
  <tag>SHOULD-HAVE</tag>
</task>

<task type="auto">
  <name>B002: VAPI/Twilio SMS Dispatch Integration (OPS-001 + LT-005)</name>
  <files>
    ops-001-staffing/src/lib/vapi.ts
    ops-001-staffing/src/pages/api/calls/incoming.ts
    ops-001-staffing/src/pages/api/calls/webhook.ts
    lt-005-courier/src/lib/sms.ts
  </files>
  <action>
Wire VAPI SDK for inbound call routing + Twilio SMS for OPS-001 staffing calls. Create: `/api/calls/incoming` POST endpoint (receives VAPI webhook for inbound call, routes to available staff via Supabase `calls` table), `/api/calls/webhook` (logs call events to Supabase). Implement OPS-001 staffing call assignment logic (query Supabase for available staff, assign call, send SMS to staff with job details). Implement LT-005 dispatch SMS (order → SMS to courier with pickup address + time). No call center UI needed for Phase 1—webhook-driven only. Test: route inbound call to test phone, verify Supabase `calls` table logs event, verify SMS sent to staff.
  </action>
  <verify>
    <automated>
curl -X POST https://ops-001.vercel.app/api/calls/webhook \
  -H "Content-Type: application/json" \
  -d '{"event":"call.started","call_id":"test123","phone":"+15551234567"}' | grep -q '"success"' && echo "PASS: Webhook received" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM calls WHERE event_type = 'call.started';" | grep -q "1" && echo "PASS: Call logged" || echo "FAIL"

# Manual: Trigger VAPI test call, verify SMS received on staff phone
echo "Manual: Receive test SMS on staff number (check operator)"
    </automated>
  </verify>
  <done>
VAPI + Twilio integrated for OPS-001 call routing and LT-005 SMS dispatch. Inbound calls route to available staff via Supabase assignment logic. SMS delivery verified. Unblocks OPS-001 revenue + LT-005 synergy S4.
  </done>
  <precondition>VAPI account created; Twilio account with SMS enabled; API keys in Vercel env</precondition>
  <tag>SHOULD-HAVE</tag>
</task>

<task type="auto">
  <name>B003: Stripe Webhooks + Supabase Payment RLS</name>
  <files>
    ec-112-cosmic-kitty/src/pages/api/webhooks/stripe.ts
    con-001-ace-construction/src/pages/api/webhooks/stripe.ts
    .supabase/migrations/004_payments_rls.sql
  </files>
  <action>
Create Stripe webhook endpoint at `/api/webhooks/stripe` to receive `payment_intent.succeeded` + `charge.refunded` events. Webhook writes to Supabase `payments` table (id, venture_id, stripe_id, amount, status, created_at). RLS: users read only their own payments; staff read venture payments; admins full access. Create migrations for `payments` table + RLS policies. Test: trigger test payment via Stripe CLI (`stripe trigger payment_intent.succeeded`), verify record appears in Supabase, verify RLS prevents cross-venture access. No checkout flow wired yet—webhook + DB only.
  </action>
  <verify>
    <automated>
curl -X POST https://ec-112.vercel.app/api/webhooks/stripe \
  -H "Content-Type: application/json" \
  -d '{"type":"payment_intent.succeeded","data":{"object":{"id":"pi_test","amount":5000}}}' | grep -q '"success"' && echo "PASS: Webhook received" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM payments WHERE stripe_id LIKE 'pi_%';" | grep -q "1" && echo "PASS: Payment recorded" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT * FROM pg_policies WHERE tablename = 'payments' AND qual ~ 'auth.uid';" | grep -q "rls" && echo "PASS: RLS enforced" || echo "FAIL"
    </automated>
  </verify>
  <done>
Stripe webhooks receive payment events; Supabase `payments` table enforces RLS per user/venture. EC-112 + CON-001 can now wire checkout flows (Phase 1 Task 6-7) without payment backend uncertainty. Unblocks $150K annual revenue.
  </done>
  <precondition>Stripe account created; Stripe API key in Vercel env; Stripe CLI authenticated</precondition>
  <tag>SHOULD-HAVE</tag>
</task>

<task type="auto">
  <name>Deploy CON-001 (Construction) to Vercel with Stripe Integration</name>
  <files>
    con-001-ace-construction/src/pages/api/invoices/create.ts
    con-001-ace-construction/src/pages/api/invoices/checkout.ts
    con-001-ace-construction/src/components/InvoiceCheckout.tsx
    con-001-ace-construction/vercel.json
  </files>
  <action>
Deploy CON-001 to Vercel (branch: con/con-001/stripe-checkout). Wire Stripe checkout: `/api/invoices/checkout` creates Stripe session, returns session ID to frontend, checkout component redirects to Stripe Hosted Checkout. Minimal invoice UI (list projects, estimate totals, checkout button). Connect to Supabase `payments` table via webhook from B003. Deploy to Vercel, set env vars (from B004), verify build succeeds. Test: create invoice → initiate checkout → verify Stripe session created. No full project tracking yet (B005 Phase 2)—invoicing only.
  </action>
  <verify>
    <automated>
curl -X POST https://con-001.vercel.app/api/invoices/checkout \
  -H "Content-Type: application/json" \
  -d '{"invoice_id":"inv_001","amount":50000}' | grep -q '"session_id"' && echo "PASS: Stripe session created" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM payments WHERE venture_id = 'con-001';" && echo "PASS: Payments table linked" || echo "FAIL"
    </automated>
  </verify>
  <done>
CON-001 live on Vercel with Stripe checkout flow. Invoices can be issued and paid via Stripe. Integration with Supabase `payments` table (B003) confirmed. Ready for ad campaigns (Task 12).
  </done>
  <precondition>B003 completed; B004 env vars set</precondition>
  <tag>MUST-HAVE</tag>
</task>

<task type="auto">
  <name>Deploy EC-112 (E-commerce) to Vercel with Medusa + Stripe</name>
  <files>
    ec-112-cosmic-kitty/src/pages/api/products/list.ts
    ec-112-cosmic-kitty/src/pages/api/cart/checkout.ts
    ec-112-cosmic-kitty/src/components/ProductCatalog.tsx
    ec-112-cosmic-kitty/src/components/Cart.tsx
    ec-112-cosmic-kitty/vercel.json
  </files>
  <action>
Deploy EC-112 storefront to Vercel (branch: ec/ec-112/medusa-stripe). Wire Medusa backend for product catalog (existing Medusa instance running locally or Railway). Checkout component: add to cart → checkout → Stripe Hosted Checkout. Connect to Supabase `payments` table via Stripe webhook (B003). Product catalog seeded with 5-10 test items (cosmetics, toys—per EC-112 brief). Deploy to Vercel, verify checkout flow end-to-end. Test: add product → checkout → Stripe payment → Supabase payment record created.
  </action>
  <verify>
    <automated>
curl https://ec-112.vercel.app/api/products/list | grep -q '"id"' && echo "PASS: Products endpoint live" || echo "FAIL"

curl -X POST https://ec-112.vercel.app/api/cart/checkout \
  -H "Content-Type: application/json" \
  -d '{"cart_id":"cart_001"}' | grep -q '"session_id"' && echo "PASS: Checkout session created" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM payments WHERE venture_id = 'ec-112';" && echo "PASS: Payments table linked" || echo "FAIL"
    </automated>
  </verify>
  <done>
EC-112 live on Vercel with full e-commerce checkout flow (products → cart → Stripe payment). Medusa integration verified. Ready for ad campaigns (Task 11).
  </done>
  <precondition>B003 completed; B004 env vars set; Medusa backend running</precondition>
  <tag>MUST-HAVE</tag>
</task>

<task type="auto">
  <name>Setup OPS-001 Call Script + VAPI Routing Logic</name>
  <files>
    ops-001-staffing/public/vapi-script.json
    ops-001-staffing/src/lib/callAssignment.ts
    ops-001-staffing/docs/CALL-SCRIPT.md
  </files>
  <action>
Create VAPI call script for OPS-001 staffing: greeting ("Connecting you to available staffing..."), hold music, staff assignment via Supabase (query available staff, assign first available), staff receives call on Twilio number. Create call assignment logic in TypeScript (`callAssignment.ts`): query Supabase for staff with `available=true`, assign call, update `calls` table with staff ID, send SMS to staff with job details (company, phone, rate). Document script in CALL-SCRIPT.md for training. No AI agent needed for Phase 1—static routing only. Test: dial OPS-001 number, verify routed to staff, staff receives SMS with job details.
  </action>
  <verify>
    <automated>
curl -X POST https://ops-001.vercel.app/api/calls/assign \
  -H "Content-Type: application/json" \
  -d '{"call_id":"call_123","incoming_phone":"+15551234567"}' | grep -q '"staff_id"' && echo "PASS: Call assigned" || echo "FAIL"

psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM calls WHERE assigned_at IS NOT NULL;" && echo "PASS: Calls assigned and logged" || echo "FAIL"

# Manual: Dial OPS-001 number, verify SMS received
echo "Manual: Call ops-001.vapi.com test number, verify SMS received on staff phone"
    </automated>
  </verify>
  <done>
OPS-001 call script live; inbound calls routed to available staff via Supabase assignment; staff notified via SMS with job details. Ready for campaign launch (Task 12).
  </done>
  <precondition>B002 completed; VAPI + Twilio configured</precondition>
  <tag>MUST-HAVE</tag>
</task>

<task type="auto">
  <name>Setup EC-112 Paid Ad Campaigns (Facebook + Google)</name>
  <files>
    ec-112-cosmic-kitty/docs/AD-STRATEGY.md
    ec-112-cosmic-kitty/public/ad-pixels.js
    ec-112-cosmic-kitty/.env.production (ad account IDs)
  </files>
  <action>
Create Facebook Ads account (if not exists) + Google Ads account for EC-112. Create 2 campaigns: (1) "EC-112 Store Traffic" — audience: US females 18-45, interest in cosmetics/gifts, daily budget $100, CPA target $10; (2) "EC-112 Retargeting" — audience: site visitors (via Facebook pixel), daily budget $50. Add Facebook Pixel to checkout page (public/ad-pixels.js) to track conversions (purchase, add-to-cart). Google conversion tracking via Stripe webhook (B003) → Google Ads API (purchases logged as conversions). Campaign start: Week 1 Day 5 (after Task 12 launch, not yet). Document strategy + budgets in AD-STRATEGY.md. Budget: $300/week ($1200 for 4 weeks, assume 10-20 conversions → $50-100K revenue potential if $500+ avg order value).
  </action>
  <verify>
    <automated>
# Verify pixel installed
grep -q "fbq('track', 'Purchase')" ec-112-cosmic-kitty/public/ad-pixels.js && echo "PASS: Facebook pixel tracked" || echo "FAIL"

# Verify Google Ads account linked (manual)
echo "Manual: Verify Facebook Ads account created + Google Ads account linked + $300/week budget allocated"

# Verify Stripe conversion tracking (B003)
psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM payments WHERE venture_id = 'ec-112';" && echo "PASS: Payments table tracks EC-112 purchases" || echo "FAIL"
    </automated>
  </verify>
  <done>
EC-112 paid ad campaigns configured: Facebook (cold traffic + retargeting) + Google (search + remarketing). Pixels installed for conversion tracking. Budget allocated ($300/week). Ready for launch (Task 12).
  </done>
  <precondition>EC-112 live on Vercel (Task 7); Facebook/Google accounts created</precondition>
  <tag>MUST-HAVE</tag>
</task>

<task type="auto">
  <name>Setup CON-001 Paid Ad Campaigns (Facebook + Google)</name>
  <files>
    con-001-ace-construction/docs/AD-STRATEGY.md
    con-001-ace-construction/public/ad-pixels.js
    con-001-ace-construction/.env.production (ad account IDs)
  </files>
  <action>
Create Facebook Ads account (if not exists) + Google Ads account for CON-001. Create 2 campaigns: (1) "CON-001 Construction Services" — audience: US males 30-65, home ownership, interest in construction/contractors, daily budget $150, CPA target $25; (2) "CON-001 Retargeting" — audience: site visitors (via Facebook pixel), daily budget $100. Add Facebook Pixel to checkout page (public/ad-pixels.js) to track lead forms + conversions. Google conversion tracking via Stripe webhook (B003) → Google Ads API. Campaign start: Week 1 Day 5 (after Task 12 launch, not yet). Document strategy + budgets in AD-STRATEGY.md. Budget: $500/week ($2000 for 4 weeks, assume 20-40 leads → $100K-$300K revenue potential if $5K+ avg project).
  </action>
  <verify>
    <automated>
# Verify pixel installed
grep -q "fbq('track', 'Lead')" con-001-ace-construction/public/ad-pixels.js && echo "PASS: Facebook pixel tracks leads" || echo "FAIL"

# Verify Google Ads account linked (manual)
echo "Manual: Verify Facebook Ads account created + Google Ads account linked + $500/week budget allocated"

# Verify Stripe conversion tracking (B003)
psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM payments WHERE venture_id = 'con-001';" && echo "PASS: Payments table tracks CON-001 invoices" || echo "FAIL"
    </automated>
  </verify>
  <done>
CON-001 paid ad campaigns configured: Facebook (cold traffic + retargeting) + Google (search + remarketing). Pixels installed for lead + conversion tracking. Budget allocated ($500/week). Ready for launch (Task 12).
  </done>
  <precondition>CON-001 live on Vercel (Task 6); Facebook/Google accounts created</precondition>
  <tag>MUST-HAVE</tag>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Tracer task deployed OPS-001 to production. Blockers B001-B003 resolved. CON-001 + EC-112 deployed to Vercel. All 3 ventures' ad campaigns configured + pixels installed. VAPI call routing + SMS dispatch live.
  </what-built>
  <how-to-verify>
1. **OPS-001 staffing portal** (tracer): Visit https://ops-001.vercel.app, submit staffing request, verify request appears in Supabase `staffing_requests` table, verify SMS sent to test staff number. Dial OPS-001 number, verify call routed to staff.
2. **CON-001 construction**: Visit https://con-001.vercel.app, create invoice for $5,000, click "Pay Now", complete Stripe test payment (card: 4242 4242 4242 4242, exp: 12/25, CVC: 123), verify payment appears in Supabase `payments` table.
3. **EC-112 e-commerce**: Visit https://ec-112.vercel.app, add product to cart, proceed to checkout, complete Stripe test payment, verify order appears in Supabase + Medusa, verify Facebook pixel fired (via browser DevTools).
4. **Supabase schema** (B001): Run `SELECT COUNT(*) FROM properties;` verify returns 3 test properties.
5. **VAPI webhooks** (B002): Trigger test call via VAPI dashboard, verify Supabase `calls` table has new record.
6. **Stripe webhooks** (B003): Trigger test payment via Stripe CLI, verify Supabase `payments` table has new record.
7. **Ad campaigns**: Verify Facebook Ads Manager shows both EC-112 + CON-001 campaigns with correct daily budgets ($300/week and $500/week respectively). Verify Google Ads shows campaigns + conversion tracking linked.
8. **Vercel deployments**: Verify all 3 ventures build + deploy successfully with env vars set. No errors in Vercel logs.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 verification checks pass, or describe any failures (e.g., "Stripe payment failed: X", "SMS not received: Y")</resume-signal>
</task>

<task type="auto">
  <name>Launch Revenue Campaigns — Go-Live (All 3 Ventures)</name>
  <files>
    .deployment-log.md (documentation only)
  </files>
  <action>
Activate all 3 revenue campaigns simultaneously on Day 5: (1) EC-112 paid ads → launch Facebook + Google campaigns (daily budgets live); (2) CON-001 paid ads → launch Facebook + Google campaigns (daily budgets live); (3) OPS-001 staffing → enable call routing + publish staffing portal landing page. Monitor: track daily revenue via Supabase `payments` table (EC-112 + CON-001 purchases) + call volume via `calls` table (OPS-001 staff assignments). Document go-live checklist in .deployment-log.md: campaigns active, budgets live, landing pages published, payment processing verified, call routing verified, team notified. Phase 1 success = $50K revenue by end of Week 1.
  </action>
  <verify>
    <automated>
# Verify campaigns are live (manual, no API)
echo "Manual: Verify Facebook Ads Manager shows campaigns in 'Active' status"
echo "Manual: Verify Google Ads Manager shows campaigns in 'Enabled' status"

# Verify revenue tracking
psql -h localhost -U postgres -d ventures -c "SELECT SUM(amount) FROM payments WHERE venture_id IN ('ec-112', 'con-001') AND created_at >= NOW() - INTERVAL '7 days';" | grep -q "[0-9]" && echo "PASS: Revenue tracked" || echo "FAIL"

# Verify call volume
psql -h localhost -U postgres -d ventures -c "SELECT COUNT(*) FROM calls WHERE venture_id = 'ops-001' AND created_at >= NOW() - INTERVAL '7 days';" | grep -q "[0-9]" && echo "PASS: Call volume tracked" || echo "FAIL"
    </automated>
  </verify>
  <done>
All 3 revenue campaigns live and generating traffic. EC-112 + CON-001 ad spend active. OPS-001 call routing live. Phase 1 Week 1 target: $50K revenue activated, 4 blockers resolved (B001-B003 done, B004 implicit in all deployments), 3 ventures deployed to Vercel. Committed as "feat(01-12): launch revenue campaigns (go-live)".
  </done>
  <precondition>All 12 tasks completed; human verification (Task 11) approved</precondition>
  <tag>MUST-HAVE</tag>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Crossing Input |
|----------|-------------|-----------------|
| Client → API | Web browser (OPS-001 portal, EC-112 storefront, CON-001 invoices) | HTTP POST/GET with user input (staffing requests, checkout cart, invoice amounts) |
| API → Supabase | Vercel serverless functions | SQL queries via Supabase client, must enforce RLS |
| API → Stripe | Webhook from external SaaS | Stripe webhook signature validation required |
| API → VAPI | Webhook from external SaaS | VAPI webhook signature validation required |
| Ad platform → Site | Facebook/Google pixels | Conversion tracking pixels, user browsing data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-S01 | Spoofing | Stripe webhook | High | Mitigate | Validate webhook signature via `stripe.webhooks.constructEvent(body, sig, secret)` in B003 endpoint before processing payment. Reject unsigned webhooks. |
| T-01-S02 | Spoofing | VAPI webhook | High | Mitigate | Validate webhook signature via VAPI SDK in B002 endpoint before routing call. Reject unsigned webhooks. |
| T-01-T01 | Tampering | Supabase RLS | High | Mitigate | Enforce RLS policies on all tables: `properties` (read: tenants own records only), `payments` (read: users own records), `calls` (read: staff own assignments). Test RLS with role-based SELECT in Task 3-5 verify steps. |
| T-01-T02 | Tampering | Vercel env vars | High | Mitigate | Never commit SUPABASE_SERVICE_ROLE_KEY or STRIPE_SECRET_KEY to git. Use `vercel env add` CLI only. Rotate keys if accidentally exposed. Document in DEPLOYMENT-CHECKLIST.md. |
| T-01-R01 | Repudiation | Invoice payments | Medium | Mitigate | Log all Stripe events to Supabase `payments` table (webhook handler in B003). Stripe webhook + Supabase record = audit trail. |
| T-01-I01 | Information Disclosure | Supabase schema | Medium | Mitigate | Supabase public schema is not readable without auth token (Supabase project API enforces JWT). Validate only anon key is leaked, not service role. |
| T-01-I02 | Information Disclosure | Ad pixels | Low | Accept | Facebook + Google pixels collect user browsing data by design (standard e-commerce). Acceptable per business model. No secrets embedded in pixels. |
| T-01-D01 | Denial of Service | Vercel API limits | Low | Mitigate | Each task sets reasonable rate limits (webhook max 100 req/sec per IP). No authentication bypass in Stripe/VAPI validate logic. Monitor Vercel function duration (no runaway loops). |
| T-01-E01 | Elevation of Privilege | RLS bypass | High | Mitigate | RLS policies tested in B001/B003 verify steps. Users cannot `UPDATE payments SET venture_id='other'` (RLS blocks cross-venture writes). Test with role switching. |
| T-01-SC | Supply Chain | npm packages | High | Mitigate | Lock package versions in package-lock.json. Stripe, VAPI, Supabase official SDKs only (no community forks). Slopcheck before Phase 2 launch. |

</threat_model>

<verification>
**Phase 1 End-to-End Verification (Week 1, Day 5)**

1. ✅ OPS-001 staffing portal live + accepting requests (tracer proves full stack)
2. ✅ CON-001 invoicing + Stripe checkout live
3. ✅ EC-112 e-commerce + Stripe checkout live
4. ✅ All 3 ventures deployed to Vercel with production URLs
5. ✅ Supabase `properties`, `tenants`, `payments`, `calls` tables initialized + RLS enforced
6. ✅ Stripe webhooks route payments to Supabase
7. ✅ VAPI + Twilio dispatch call routing + SMS live
8. ✅ Facebook + Google ad campaigns active with $300/week (EC-112) + $500/week (CON-001) budgets
9. ✅ $50K revenue target tracked (or on track by end of Day 5)
10. ✅ Portfolio readiness: 27.7% → 35%+

**Blocker Resolution Audit:**
- B001 ✅ Resolved (property/tenant schema + RLS)
- B002 ✅ Resolved (VAPI + Twilio SMS dispatch)
- B003 ✅ Resolved (Stripe webhooks + RLS)
- B004 ✅ Resolved (Vercel env vars set)
- B005 🚫 Not Phase 1 (construction project tracking deferred to Phase 2)

**Synergy Activation Status:**
- S1 (B001 + RE-001 → LT-005 lease): Ready Phase 2 (B001 done, B005 pending)
- S2 (B003 + EC-112 → FIN-001 subs): Ready Phase 4 (B003 done)
- S3 (CON-001 → RE-001 valuations): Ready Phase 2 (CON-001 live, B005 pending)
- S4 (OPS-001 → LT-005 dispatch): Ready Phase 2 (B002 done)
- S5 (LT-011 platform): Ready Phase 4 (B004 done)
- S6 (FIN-037 trading): Ready Phase 4 (B003 done)
</verification>

<success_criteria>
✅ **Week 1 (Phase 1) Complete When:**

1. **Revenue Activated**: $50K cumulative revenue from OPS-001 (calls) + EC-112 (e-commerce) + CON-001 (invoices)
2. **Blockers Resolved**: B001-B004 unblocked, B005 deferred to Phase 2
3. **Ventures Deployed**: OPS-001, CON-001, EC-112 live on Vercel with production URLs
4. **Campaigns Live**: EC-112 + CON-001 paid ads active with tracking pixels + conversion logging
5. **Database Models**: Supabase `properties`, `tenants`, `payments`, `calls` tables with RLS enforced
6. **Integrations Verified**: Stripe webhooks, VAPI call routing, SMS dispatch all tested end-to-end
7. **Readiness Improvement**: Portfolio average readiness 27.7% → 35% (recomputed via VENTURE-READINESS-SCORECARD)
8. **Team Notified**: All venture teams have live landing pages, live payment processing, live call/lead routing

**Not Required for Phase 1 (Defer to Phase 2+):**
- Full project tracking (CON-001) — B005 Phase 2
- LT-011 platform ops integration — Phase 4
- Advanced analytics + dashboards — Phase 3
- Multi-tenant architecture — Phase 4
</success_criteria>

<output>
Create `.planning/phases/01-launch-campaigns/01-01-SUMMARY.md` after phase execution completes. Summary to include: actual vs. target revenue, blocker resolution timeline, venture readiness audit, synergy activation readiness, go-live checklist completion.
</output>

